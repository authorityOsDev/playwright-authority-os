
# Environment — never commit real secrets
.env
.env.local
.env.*.local

# Auth session — auto-generated, auto-deleted
.auth/

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/allure-report/
/allure-results/
# Visual regression baselines — platform-specific, each environment generates its own
# Run: npx playwright test --update-snapshots   to create/refresh the baseline
*-snapshots/
