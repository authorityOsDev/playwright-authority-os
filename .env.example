# -------------------------------------------------------
# Authority OS — Environment Configuration Template
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# -------------------------------------------------------

# Target environment (staging | prod | dev)
ENV=staging

# Base URL of the application under test
BASE_URL=https://www.saucedemo.com

# Test account credentials
STANDARD_USER=standard_user
PASSWORD=secret_sauce

# Checkout form data (used by checkout tests)
CHECKOUT_FIRST_NAME=Jane
CHECKOUT_LAST_NAME=Doe
CHECKOUT_ZIP=10001

# API authentication token (required for authenticated API tests)
API_TOKEN=

# Auth session storage path (default used by playwright.config.ts)
STORAGE_STATE=.auth/user.json
